interface:
  display_name: "opencli-sequential-news-zh"
  short_description: "Sequential opencli news pipeline with URL dedupe"
  default_prompt: "Run configured opencli news commands sequentially, skip failed commands, normalize and dedupe by URL globally, then translate deduped titles to Chinese in-model and export YYYY-MM-DD-HH-mm_fullNews.md with an errors section."
